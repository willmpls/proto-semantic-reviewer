# Proto Semantic Reviewer - Environment Configuration
# Copy this file to .env and fill in your values.
# All variables in this file will be passed to the Docker container.

# =============================================================================
# API Keys (at least one required)
# =============================================================================
# GOOGLE_API_KEY=your-gemini-api-key
# OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# =============================================================================
# Provider Selection (optional)
# =============================================================================
# Force a specific provider instead of auto-detecting from available keys
# MODEL_PROVIDER=openai

# =============================================================================
# Custom Base URLs (optional)
# =============================================================================
# Override API endpoints for proxies or private deployments
# OPENAI_BASE_URL=https://proxy.example.com/openai/v1
# ANTHROPIC_BASE_URL=https://proxy.example.com/anthropic
# GEMINI_BASE_URL=https://proxy.example.com/gemini

# =============================================================================
# Custom HTTP Headers (optional)
# =============================================================================
# Add custom headers to API requests. Format: {PROVIDER}_HEADER_{NAME}=value
# - Single underscore _ becomes hyphen -
# - Double underscore __ becomes literal underscore _
#
# Examples:
# OPENAI_HEADER_X_Custom_Auth=my-auth-token
# OPENAI_HEADER_X_Trace_Id=trace-123
# ANTHROPIC_HEADER_X_Request_Id=req-456

# =============================================================================
# Custom CA Certificates (optional)
# =============================================================================
# For corporate proxies or self-signed certificates
# LLM_CA_BUNDLE=/path/to/ca-bundle.pem
# OPENAI_CA_BUNDLE=/path/to/openai-ca.pem
# ANTHROPIC_CA_BUNDLE=/path/to/anthropic-ca.pem
# GEMINI_CA_BUNDLE=/path/to/gemini-ca.pem

# =============================================================================
# Authorization (optional)
# =============================================================================
# Comma-separated list of AD groups allowed to access the API
# ALLOWED_AD_GROUPS=team-platform,team-backend

# =============================================================================
# Other Settings
# =============================================================================
# Custom standards directory
# STANDARDS_DIR=/app/standards

# API timeout in seconds (default: 120)
# LLM_TIMEOUT=120
